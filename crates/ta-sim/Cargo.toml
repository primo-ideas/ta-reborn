[package]
name = "ta-sim"
description = "Deterministic lockstep simulation (no Bevy, no floats)."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
